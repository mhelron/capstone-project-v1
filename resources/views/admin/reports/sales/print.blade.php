<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sales Report List PDF</title>
    <link rel="icon" href="data:;base64,="> <!-- Disable Laravel favicon -->
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin: 0;
            padding: 40px 20px;
        }

        .card {
            margin: auto;
            width: 90%;
            margin-top: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th, td {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 6px;
        }

        h1, h2 {
            font-size: 1.5em;
        }

        .section-header {
            font-weight: bold;
            margin: 10px 0;
        }

        .header {
            text-align: center;
            font-size: 0.8em;
            margin-bottom: 20px;
        }

        .header p {
            margin: 0;
        }

        .page-break {
            page-break-before: always;
        }

        @media print {
            .page-break {
                page-break-before: always;
            }
        }
    </style>
</head>
<body>
<div class="header">
    <p>Generated by: {{ session('fname') }}</p>
    <p>Date Generated: {{ now() }}</p>
</div>

<div class="card">
    <h1>Kyla and Kyle Catering Services</h1>
    <h2>Sales Report</h2>

    @php
        $sortedYears = $yearlyData;
        krsort($sortedYears);
    @endphp

    @foreach($sortedYears as $year => $data)
    <div class="{{ $loop->first ? '' : 'page-break' }}">
        <!-- Yearly Summary -->
        <div class="section-header">Yearly Summary ({{ $year }})</div>
        <table>
            <thead>
                <tr>
                    <th>Category</th>
                    <th>Amount</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Total Reservation Fees</td>
                    <td>₱{{ number_format($data['yearlyFees'], 2) }}</td>
                </tr>
                <tr>
                    <td>Total Package Sales</td>
                    <td>₱{{ number_format($data['yearlyPrices'], 2) }}</td>
                </tr>
                <tr>
                    <td><strong>Total Sales</strong></td>
                    <td><strong>₱{{ number_format($data['yearlyFees'] + $data['yearlyPrices'], 2) }}</strong></td>
                </tr>
            </tbody>
        </table>

        <!-- Monthly Summary -->
        <div class="section-header">Monthly Summary</div>
        <table>
            <thead>
                <tr>
                    <th>Month</th>
                    <th>Reservation Fees</th>
                    <th>Package Sales</th>
                    <th>Total</th>
                </tr>
            </thead>
            <tbody>
                @foreach($months as $index => $month)
                <tr>
                    <td>{{ $month }}</td>
                    <td>₱{{ number_format($data['reservationFees'][$index], 2) }}</td>
                    <td>₱{{ number_format($data['totalPrices'][$index], 2) }}</td>
                    <td>₱{{ number_format($data['reservationFees'][$index] + $data['totalPrices'][$index], 2) }}</td>
                </tr>
                @endforeach
            </tbody>
        </table>

        <!-- Weekly Summary for Each Month -->
        @foreach($months as $index => $month)
        <div class="section-header">Weekly Summary - {{ $month }}</div>
        <table>
            <thead>
                <tr>
                    <th>Week</th>
                    <th>Reservation Fees</th>
                    <th>Package Sales</th>
                    <th>Total</th>
                </tr>
            </thead>
            <tbody>
                @for($week = 0; $week < 4; $week++)
                <tr>
                    <td>Week {{ $week + 1 }}</td>
                    <td>₱{{ number_format($data['weeklyData'][$index + 1]['weeklyFees'][$week], 2) }}</td>
                    <td>₱{{ number_format($data['weeklyData'][$index + 1]['weeklyPrices'][$week], 2) }}</td>
                    <td>₱{{ number_format($data['weeklyData'][$index + 1]['weeklyFees'][$week] + $data['weeklyData'][$index + 1]['weeklyPrices'][$week], 2) }}</td>
                </tr>
                @endfor
            </tbody>
        </table>
        @endforeach
    </div>
    @endforeach
</div>

<script>
    window.onload = function () {
        window.print();
    };
</script>
</body>
</html>
