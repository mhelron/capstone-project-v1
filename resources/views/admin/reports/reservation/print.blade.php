<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <title>Reservation Report</title>
    <style>
        @page {
            margin: 0cm 0cm;
        }
        
        body {
            font-family: DejaVu Sans, sans-serif;
            margin: 0;
            padding: 20px;
            margin-bottom: 30px;
            font-size: 12px;
        }

        main {
            margin-bottom: 50px;
        }
        
        .header {
            text-align: center;
            margin-bottom: 20px;
        }

        .company-name {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .report-title {
            font-size: 20px;
            margin-bottom: 20px;
        }

        .meta-data {
            font-size: 12px;
            margin-bottom: 5px;
        }

        .card {
            margin: 20px auto;
            width: 100%;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
            page-break-inside: auto;
        }

        tr {
            page-break-inside: avoid;
            page-break-after: auto;
        }

        th, td {
            border: 1px solid #000;
            padding: 8px;
            text-align: left;
            font-size: 12px;
        }

        th {
            background-color: #f0f0f0;
            font-weight: bold;
        }

        .section-header {
            font-weight: bold;
            margin: 15px 0;
            font-size: 16px;
            background-color: #f8f8f8;
            padding: 5px;
            border: 1px solid #ddd;
        }

        .monthly-section {
            margin-bottom: 20px;
        }

        .page-break {
            page-break-before: always;
        }

        #footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            height: 30px;
            text-align: right;
            font-size: 10px;
            padding: 10px 20px;
            background-color: #fff;
            border-top: 1px solid #ddd;
        }

        .page-number:before {
            content: counter(page);
        }

        .page-total:before {
            content: counter(pages);
        }
    </style>
</head>
<body>
    <main>
        <div class="header">
            <div class="company-name">Kyla and Kyle Catering Services</div>
            <div class="report-title">Reservation Report</div>
            <div class="meta-data">Generated by: {{ session('fname') }} {{ session('lname') }}</div>
            <div class="meta-data">Date Generated: {{ now()->format('F d, Y h:i:s A') }}</div>
            @if(isset($filters))
                @if($filters['report_type'])
                    <div class="meta-data">Report Type: {{ ucfirst($filters['report_type']) }}</div>
                @endif
                @if($filters['year'])
                    <div class="meta-data">Year: {{ $filters['year'] }}</div>
                @endif
                @if($filters['month'])
                    <div class="meta-data">Month: {{ $months[$filters['month'] - 1] }}</div>
                @endif
                @if($filters['week'])
                    <div class="meta-data">Week: {{ $filters['week'] }}</div>
                @endif
            @endif
        </div>

        <div class="card">
            @php
                $sortedYears = $yearlyData;
                krsort($sortedYears);
            @endphp

            @foreach($sortedYears as $year => $count)
                @if(!isset($filters['year']) || $filters['year'] == $year)
                    @if(!$loop->first)
                        <div class="page-break"></div>
                    @endif

                    <!-- Yearly Summary -->
                    @if(!isset($filters['report_type']) || $filters['report_type'] == 'all' || $filters['report_type'] == 'yearly')
                        <div class="section-header">Yearly Summary ({{ $year }})</div>
                        <table>
                            <thead>
                                <tr>
                                    <th>Year</th>
                                    <th>Total Reservations</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>{{ $year }}</td>
                                    <td>{{ $count }}</td>
                                </tr>
                            </tbody>
                        </table>
                    @endif

                    <!-- Monthly Summary -->
                    @if(!isset($filters['report_type']) || $filters['report_type'] == 'all' || $filters['report_type'] == 'monthly')
                        <div class="section-header">Monthly Summary</div>
                        <table>
                            <thead>
                                <tr>
                                    <th>Month</th>
                                    <th>Reservations</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach($months as $index => $month)
                                    @if(!isset($filters['month']) || $filters['month'] == $index + 1)
                                        <tr>
                                            <td>{{ $month }}</td>
                                            <td>{{ $monthlyTrends[$year][$index] }}</td>
                                        </tr>
                                    @endif
                                @endforeach
                                <tr>
                                    <td><strong>Total</strong></td>
                                    <td><strong>{{ array_sum($monthlyTrends[$year]) }}</strong></td>
                                </tr>
                            </tbody>
                        </table>
                    @endif

                    <!-- Weekly Summary -->
                    @if(!isset($filters['report_type']) || $filters['report_type'] == 'all' || $filters['report_type'] == 'weekly')
                        <div class="section-header">Weekly Summary</div>
                        @foreach($months as $index => $month)
                            @if(!isset($filters['month']) || $filters['month'] == $index + 1)
                                <div class="monthly-section">
                                    <div class="section-header">{{ $month }}</div>
                                    <table>
                                        <thead>
                                            <tr>
                                                <th>Week</th>
                                                <th>Reservations</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            @php
                                                $totalWeeks = $weeklyData[$year][$index + 1]['totalWeeks'] ?? 4;
                                            @endphp
                                            @for($week = 1; $week <= $totalWeeks; $week++)
                                                @if(!isset($filters['week']) || $filters['week'] == $week)
                                                    <tr>
                                                        <td>Week {{ $week }}</td>
                                                        <td>{{ $weeklyData[$year][$index + 1][$week] ?? 0 }}</td>
                                                    </tr>
                                                @endif
                                            @endfor
                                        </tbody>
                                    </table>
                                </div>
                            @endif
                        @endforeach
                    @endif
                @endif
            @endforeach
        </div>
    </main>
</body>
</html>